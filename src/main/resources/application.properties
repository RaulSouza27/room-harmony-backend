spring.application.name=harmony

server.port=3313

# spring.datasource.url=jdbc:postgresql://localhost:7755/postgres
# spring.datasource.username=psiUser
# spring.datasource.password=psi123
# spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:7755}/${DB_NAME:PsiDb}
spring.datasource.username=${DB_USER:psiUser}
spring.datasource.password=${DB_PASSWORD:psi123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

app.version=${APP_VERSION:1.0.0}
app.key=${APP_KEY:minha-chave-secreta-super-segura-de-32-chars!}